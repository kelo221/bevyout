[bevy](../index.html)::[audio](index.html)

# Type Alias SampleRate 

[Source](https://docs.rs/rodio/0.22.2/x86_64-unknown-linux-gnu/src/rodio/common.rs.html#5)

```rust
pub type SampleRate = NonZero<u32>;
```

Sample rate (a frame rate or samples per second per channel).

## Aliased Type

```rust
pub struct SampleRate(/* private fields */);
```