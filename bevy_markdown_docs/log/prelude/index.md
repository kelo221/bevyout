[bevy](../../index.html)::[log](../index.html)

# Module prelude 

[Source](https://docs.rs/bevy_log/0.19.0/x86_64-unknown-linux-gnu/src/bevy_log/lib.rs.html#33)

The log prelude.

This includes the most common types in this crate, re-exported for your convenience.

## Macros

[debug](macro.debug.html "macro bevy::log::prelude::debug")

Constructs an event at the debug level.

[debug\_once](macro.debug_once.html "macro bevy::log::prelude::debug_once")

Call [`debug!`](../../prelude/macro.debug.html "macro bevy::prelude::debug") once per call site.

[debug\_span](macro.debug_span.html "macro bevy::log::prelude::debug_span")

Constructs a span at the debug level.

[error](macro.error.html "macro bevy::log::prelude::error")

Constructs an event at the error level.

[error\_once](macro.error_once.html "macro bevy::log::prelude::error_once")

Call [`error!`](../../prelude/macro.error.html "macro bevy::prelude::error") once per call site.

[error\_span](macro.error_span.html "macro bevy::log::prelude::error_span")

Constructs a span at the error level.

[info](macro.info.html "macro bevy::log::prelude::info")

Constructs an event at the info level.

[info\_once](macro.info_once.html "macro bevy::log::prelude::info_once")

Call [`info!`](../../prelude/macro.info.html "macro bevy::prelude::info") once per call site.

[info\_span](macro.info_span.html "macro bevy::log::prelude::info_span")

Constructs a span at the info level.

[once](macro.once.html "macro bevy::log::prelude::once")

Call some expression only once per call site.

[trace](macro.trace.html "macro bevy::log::prelude::trace")

Constructs an event at the trace level.

[trace\_once](macro.trace_once.html "macro bevy::log::prelude::trace_once")

Call [`trace!`](../../prelude/macro.trace.html "macro bevy::prelude::trace") once per call site.

[trace\_span](macro.trace_span.html "macro bevy::log::prelude::trace_span")

Constructs a span at the trace level.

[warn](macro.warn.html "macro bevy::log::prelude::warn")

Constructs an event at the warn level.

[warn\_once](macro.warn_once.html "macro bevy::log::prelude::warn_once")

Call [`warn!`](../../prelude/macro.warn.html "macro bevy::prelude::warn") once per call site.

[warn\_span](macro.warn_span.html "macro bevy::log::prelude::warn_span")

Constructs a span at the warn level.