# Copy this file to .bevyout/config.toml and edit the paths for your machine.

[fallout3]
game_root = "C:/Games/Fallout 3 goty"
plugin = "Fallout3.esm"

[tools]
# Optional: omitted values use automatic detection (Windows default install
# paths, or "magick"/"ktx" on PATH).
# Unified KTX-Software packages prepared D32 point-shadow arrays on a cache
# miss and packages irradiance atlases during `bake`.
ktx = "C:/Program Files/KTX-Software/bin/ktx.exe"

[output]
cache_dir = ".bevyout/cache"

[world]
# Runtime cell map and predictive door-graph neighbor preloader (issue #51).
# Interior default: 4 resident cells.
resident_cell_limit = 4
# Exterior default: 25 resident cells, matching a 5x5 uGrids-style window.
# The hard byte budget remains active as a second bound.
exterior_resident_cell_limit = 25
