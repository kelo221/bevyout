[package]
name = "bevyout"
version = "0.1.0"
edition = "2024"

[dependencies]
# WAV covers the ambient and interaction clips staged by the importer. MP3
# metadata is retained in manifests, but Bevy's MP3 feature currently resolves
# to an unavailable symphonia patch in this workspace's offline cache.
bevy = { version = "0.19.0", features = ["wav", "pbr_specular_textures"] }
avian3d = "0.7"
bevy-tnua = "0.32"
bevy-tnua-avian3d = "0.12"
esplugin = "6.1.4"
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
flate2 = "1.1"
ron = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
toml = "0.8"

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3
