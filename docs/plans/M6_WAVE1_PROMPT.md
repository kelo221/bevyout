# M6 wave 1 prompt

Establish the exterior conversion decision and prepare the repository for the
M6 world route. Native Rust NIF preparation is the production path. The old
Blender script remains only as an explicitly requested legacy preview/reference
asset; exterior preparation and runtime streaming must never invoke it.

Deliver the first deterministic exterior contracts and visible diagnostics:

- native-only conversion report and CLI policy;
- LAND/ROAD/NAVM ownership fields at the ESM4 boundary;
- revisioned coordinate, worldspace-index, cell-package, terrain, water,
  environment, and residency contracts;
- exterior package preparation and prepared-manifest integration;
- deterministic `exterior-catalog` output;
- the first prepared-package loader and bounded residency authority.

The remaining native gaps must be stated in the plan amendment and in the
top-level comment of `src/vsa/assets/blender_script.py`.
