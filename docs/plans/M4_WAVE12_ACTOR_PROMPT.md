# M4 wave 12 actor animation game-flow kickoff (#104, #106)

Requested on 2026-07-20 after actor KF compatibility PR #183 merged:

- pull the merged changes;
- create a new isolated worktree because another agent owns the original
  checkout; and
- continue the two actor-animation items identified by the integration review:
  close the measured compatibility spike (#104) and wire its prepared animation
  packs into normal gameplay actors (#106).

The continuation worktree is
`/Users/simon/projects/bevyout-worktrees/actor-animation-gameflow` on branch
`m4-actor-animation-gameflow`, created from `origin/master` at `dd757c9`.
The original checkout and the merged `Animations` worktree are outside this
wave's write scope.
