# M4 wave 13 actor state and persistence kickoff (#110)

Requested on 2026-07-20 after actor-animation game-flow PR #187 was accepted:

- continue milestone 4 in parallel while another agent integrates actor
  locomotion with navigation;
- take issue #110, covering actor values, factions, canonical equipment,
  lifecycle, and persistence; and
- keep the original checkout untouched by working in a new isolated worktree.

The continuation worktree is
`/Users/simon/projects/bevyout-worktrees/actor-state-persistence` on branch
`m4-actor-state-persistence`, created from `origin/master` at `62797f8`.
The original checkout is outside this wave's write scope.
