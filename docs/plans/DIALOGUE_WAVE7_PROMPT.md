# Dialogue Wave 7 prompt — first generated Fallout conversation

Generate one traceable Fallout conversation only after the inventory report
shows a safe initial scope.

- Keep native authored Yarn and imported Fallout modes separate.
- Generate deterministic Yarn nodes with canonical string keys and source
  mapping back to plugin/FormID/record identity.
- Route eligibility through the shared condition evaluator.
- Route results through shared action/script/quest services.
- Preserve generated/authored overlay precedence without modifying generated
  files.
- Start with one speaker, one opening response, two choices, basic conditions,
  and one result mutation.
